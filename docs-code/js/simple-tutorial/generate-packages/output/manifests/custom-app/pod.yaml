apiVersion: "v1"
kind: "Pod"
metadata:
  name: "custom-app"
  namespace: "custom-namespace"
spec:
  containers:
    - name: "echo"
      image: "hashicorp/http-echo:1.0"
      ports:
        - name: "http"
          containerPort: 80
          protocol: "TCP"
