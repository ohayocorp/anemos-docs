apiVersion: "v1"
kind: "Pod"
metadata:
  name: "example-app"
  namespace: "default"
  labels:
    app: "example-app"
spec:
  containers:
    - name: "app"
      image: "nginx"
      ports:
        - containerPort: 80
